# Add that to the path
export PATH=/usr/local/bin:$PATH
export PATH=/usr/local/sbin:$PATH
export PATH=/Applications/Sublime\ Text\ 2.app/Contents/SharedSupport/bin:$PATH
export PATH=/usr/local/share/npm/bin:$PATH

# WP CLI
PATH=$HOME/.wp-cli/bin:$PATH

# Last to override any other those other ones
export PATH=~/Dropbox/sync/bin:$PATH

SYNC_BASE=$(dirname $0)
P_DIR=~/Web/Projects
WEB_DIR=~/Web

# Set editor to sublime
export EDITOR='subl'
alias push='git push'
alias rmf='rm -rf'
alias co='commit'
alias pull='git pull'
alias fetch='git fetch'
alias branch='git branch'
alias pullr='git pull --rebase'
alias up="git config --global alias.up '!git remote update -p; git merge --ff-only @{u}'"

#source $(brew --prefix php-version)/php-version.sh && php-version 5.5

alias phpv='php-version'
